language: swift
osx_image: xcode8.3

before_install:
    - brew update
    - brew outdated xctool || brew upgrade xctool
script: xctool -workspace BarracksClient.xcworkspace -scheme "Barracks OSX" run-tests

